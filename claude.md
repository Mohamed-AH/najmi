The CSV contains the titles and details of the lectures from the site, while the TXT file contains the paths of the files that were actually downloaded. I have since converted these files to .m4a.
I need to:

Identify which files were not downloaded by cross-checking the CSV against the TXT file (report anything present in the CSV but missing from the TXT), and check if any file names changed.
Update the CSV so that the file extension reflects .m4a with the correct file name.
please give me robust scripts for above task , with progress report.
